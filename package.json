{
  "name": "medvision-workspace-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix \"medical_image_diagnosis_assistant-web app\" && npm run build --prefix \"medical_image_diagnosis_assistant-web app\""
  }
}
